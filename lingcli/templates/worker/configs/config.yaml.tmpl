# {{.ProjectName}} Worker 配置

app:
  name: {{.ProjectName}}
  environment: dev

logging:
  level: info
  filename: logs/worker.log
  maxSize: 100
  maxAge: 30
  maxBackups: 10
  daily: true
  sensitiveFields: ""

tasks:
  sampleInterval: 30s
  healthInterval: 60s
