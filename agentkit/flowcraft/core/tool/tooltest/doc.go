// Package tooltest provides reusable fixtures and contract suites for
// core/tool implementations.
//
// It is modeled on core/agent/agenttest and core/inference/inferencetest:
// a sibling package that implementation tests may import, but production
// code never does.
package tooltest
